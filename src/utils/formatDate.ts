export const formatMouth = Intl.DateTimeFormat('pt-br',{
    month: 'long'
})

export const formatWeek = Intl.DateTimeFormat('pt-br', {
    weekday: 'long'
})

export const formatMonthAndDay = Intl.DateTimeFormat('pt-br', {
    day: 'numeric',
    month: 'long',
    
})

